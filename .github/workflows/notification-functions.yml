name: Build and Deploy Notification Functions

on:
  push:
    branches:
      - main
    paths:
      - 'src/Services/Notification/**'
  workflow_dispatch:

env:
  ACR_LOGIN_SERVER: 'expenseflowregistry.azurecr.io'
  IMAGE_NAME: 'expenseflow/notification-functions'
  AZURE_FUNCTION_APP_NAME: 'expenseflow-notification-functions'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Azure Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.ACR_LOGIN_SERVER }}
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}

    - name: Build and Push Docker Image
      uses: docker/build-push-action@v6
      with:
        context: .
        file: ./src/Services/Notification/ExpenseFlow.Notification.Functions/Dockerfile
        push: true
        tags: |
          ${{ env.ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          ${{ env.ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Deploy to Azure Functions App
      uses: azure/webapps-deploy@v3
      with:
        app-name: ${{ env.AZURE_FUNCTION_APP_NAME }}
        publish-profile: ${{ secrets.AZURE_FUNCTIONS_PUBLISH_PROFILE }}
        images: '${{ env.ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:${{ github.sha }}'
